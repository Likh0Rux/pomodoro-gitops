apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "pomodoro.fullname" . }}-html
  labels:
    {{- include "pomodoro.labels" . | nindent 4 }}
data:
  index.html: |
{{ .Files.Get "files/index.html" | indent 4 }}
